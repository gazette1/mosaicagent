services:
  - type: web
    name: mosaic-intake
    runtime: node
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: node tools/demo-server.js
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: DEMO_PASSCODE
        sync: false
